/*
 * To change this license header, choose License Headers in Project Properties.
 * To change this template file, choose Tools | Templates
 * and open the template in the editor.
 */
package ca.sheridancollege.project;

/**
 *
 * @author Jaeyoung
 */
public class discardCard {
    // discardCard method could be boolean
    public boolean discard(int[] hand){
        // suit of the card is irrelvant for it to be filled... All we have to check is the pure value (0 ~ 10).'
        // if it is either queen or jack, it is an automatic discard
        //int i = 0;
        //while ( i < hand.length){
        //    if (hand[i].getValue() == Card.QUEEN || hand[i].getValue() == Card.Jack){
        //        return true
        //    }
        //    else{
                // else: filled authentcation... if it is already filled, discard... if not, put into sequence (also discarded though).
        //    }
    //    }
    }
}
